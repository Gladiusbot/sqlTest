```sql
create table BILLS(
    NAME CHAR(30) NOT NULL,
	AMOUNT INT,
	ACCOUNT_ID INT NOT NULL);

create table BANK_ACCOUNTS(
	ACCOUNT_ID INT NOT NULL,
	TYPE CHAR(30),
	BALANCE INT,
	BANK CHAR(30));

create table COMPANY(
	NAME CHAR(30) NOT NULL,
	ADDRESS CHAR(50),
	CITY CHAR(30),
	STATE CHAR(2));
```
